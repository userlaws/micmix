# MicMix

## Working agreement
- Build a Windows 11 Electron + React + TypeScript desktop app in phases below.
- Stop at every CHECKPOINT. Only the user can confirm audible results; never claim an audio test passed without their report.
- Commit each phase. Read this file before continuing after a context reset.
- Prefer boring, reliable code. No native Node modules, Rust, Python, accounts, cloud, auto-updater, EQ, plugins, Spotify, yt-dlp, or media downloading.
- Stop for actions requiring the user's hearing, an admin prompt, or a reboot; provide exact instructions.

## Architecture
- Main process owns windows, IPC, embedded YouTube BrowserView, globalShortcut, persistence, integration, and installer/repair entry points.
- One visible React UI: dark single window; LIVE/OFF AIR, Mic/Music/Soundboard strips and master, central music/queue/video panel, right soundboard, bottom device/Discord/FiveM status, one settings gear. No nested menus.
- A separate hidden renderer owns all Web Audio processing and device enumeration. No UI rendering, animations, or meters in that window. UI communicates over narrow typed IPC.
- Virtual output AudioContext: latencyHint interactive, setSinkId to the output whose label contains CABLE Input. Second AudioContext for monitoring on real playback hardware. Default monitor excludes mic; optional include-mic toggle.
- User-selected mic getUserMedia disables echoCancellation, noiseSuppression, and autoGainControl.
- Source GainNodes -> master GainNode -> DynamicsCompressorNode limiter (threshold -1 dB, ratio 20, fast attack). Optional mono downmix on virtual output.
- Mic RMS drives music auto-duck with configurable threshold/depth (default -12 dB), 50 ms attack and 400 ms release using setTargetAtTime. Toggle in settings.
- AnalyserNode peak meters per source, reported at 20 Hz. Handle device disappearance with banner and re-enumeration, never a crash.
- Local mp3/wav/flac/ogg use audio elements + MediaElementSourceNode; drag/drop, queue, play/pause/skip.
- YouTube embed URL youtube.com/embed/ID?enablejsapi=1 in BrowserView, IFrame API postMessage for playback/seek/queue; Chromium autoplay policy disabled. Requested capture is desktopCapturer/setDisplayMediaRequestHandler for that webContents, with own output muted. Verify Electron/Windows support before implementation: never substitute system-wide loopback or claim muted capture works without testing. Embed failures show: "This video blocks embedding, try another".
- Soundboard decodes local clips once into AudioBuffers; globalShortcut hotkeys.

## Installation and integrations
- electron-builder NSIS; standard VB-CABLE only, package from vb-audio.com in build/vbcable. Verify redistribution and silent-install terms before bundling. Include donationware attribution and donation link in installer and About.
- Installer elevates once, installs driver, restarts Windows Audio service via net stop audiosrv / net start audiosrv, checks devices, offers reboot if needed. App never elevated. Uninstaller offers VB-CABLE removal.
- UI calls cable "MicMix Virtual Mic" except diagnostics and guided instructions which must show real Windows names.
- First launch confirms CABLE Input and Output, Repair if missing; defaults to default mic/playback, test tone plus meter. Save settings; wizard repeats only if cable disappears.
- Discord official local RPC ports 6463–6472: GET_VOICE_SETTINGS, remember previous input, SET_VOICE_SETTINGS to CABLE Output, connected indicator and revert. Verify official authorization requirements and restoration behavior; restore explicitly if needed, disconnect on exit. Never edit Discord files. Fall back to exact manual menu path when unavailable.
- Detect FiveM process; guided Settings > Voice Chat > Input Device > CABLE Output only, no automation.

## Phases and checkpoints
0. Toolchain; enumerate and print real audio devices from Electron. CHECKPOINT: user confirms CABLE devices appear.
1. Mic -> Web Audio -> CABLE Input and test tone. CHECKPOINT: user selects CABLE Output in Discord and uses mic test.
2. Local files, mixer, ducking, limiter, monitor, meters. CHECKPOINT: file + mic + ducking in Discord.
3. YouTube panel and captured audio. CHECKPOINT: paste link, play, friend hears it.
4. Soundboard, hotkeys, first-launch, Discord RPC, FiveM card, settings persistence. CHECKPOINT: delete config and simulate fresh install.
5. Bundled VB-CABLE installer; README five-step guide and troubleshooting for missing CABLE, silent Discord, robotic audio, double monitoring, blocked embeds.

## Current state
- Phase 0 PASSED: user supplied Electron log with CABLE Input and CABLE Output and authorized Phase 1.
- Phase 1 PASSED: user confirmed hearing their voice, ringing resolved after routing guidance, and OFF AIR silences / LIVE restores voice. User authorized Phase 2.
- Phase 2 implemented; STOP at file + mic + ducking Discord checkpoint. Phase 2 audible quality, physical device disconnects, and subjective monitoring latency remain unconfirmed.
- Phase 3 implemented; STOP at the paste-link / play / friend-hears-it checkpoint. No audible Phase 3 result is confirmed. Phase 2's Discord checkpoint is also still unconfirmed by the user.
- Phase 3 CHECKPOINT PASSED (2026-09-09): a friend in Discord heard the YouTube audio plus the user's mic, loud. Level-bounce feedback addressed by full-level defaults, -30 dBFS duck threshold, 150 ms limiter release, and a visible duck toggle.
- Phase 4 implemented (soundboard + hotkeys, first-launch wizard, FiveM/Discord status, settings/queue/pad persistence). STOP at the fresh-install checkpoint: user deletes %APPDATA%\MicMix\config.json, relaunches, and confirms the wizard runs once then devices/faders/queue/pads survive a restart and hotkeys fire from other apps. No audible Phase 4 result is confirmed.
- Phase 4 code: src/config.ts (atomic JSON in userData, field-by-field validation, corrupt file -> defaults), src/hotkeys.ts (accelerator validation + KeyboardEvent.code mapping; bare keys rejected so typing is never hijacked), src/processes.ts (tasklist-based Discord/FiveM detection, no automation), src/soundboard-panel.tsx, src/wizard.tsx, pad/config IPC and globalShortcut in src/main.ts, pad decode/replay in src/passthrough.ts.
- Soundboard: up to 9 pads, each a local clip decoded once into an AudioBuffer in the worker and mixed under the existing soundboard gain (so it reaches Discord and obeys master/limiter/mono/monitor). Tap to play, tap again to stop, auto-stop on end. Clips <= 60 s and file <= 30 MB. Pads and hotkeys owned by main; the UI cannot inject pad definitions (audio:command rejects type 'pads').
- Hotkeys are Electron globalShortcut accelerators requiring a modifier (Ctrl/Alt/Shift) or an F-key/numpad key; duplicates and OS-claimed combos are rejected. Registered while the app runs (LIVE gate is enforced at play time), unregistered on quit.
- Persistence: main owns config.json; worker state (settings, queue) mirrors into it after a one-time restore, debounced 500 ms and flushed on quit. Devices are saved and restored by LABEL because Chromium device IDs do not survive a restart. The queue is NOT persisted (user request 2026-09-09: a restored YouTube video looked like a stray track on launch); config.queue is kept in the schema but always saved empty. Missing local files are dropped from pads without error. Restart always comes up OFF AIR with an empty Music panel.
- Discord RPC NOT implemented, by decision: docs.discord.com/developers/topics/rpc confirms RPC is gated to app owner + up to 50 approved testers until Discord approves the app, AUTHENTICATE needs a server-side OAuth2 token exchange (client_secret) or an rpc_token that also needs approval, and the WebSocket transport verifies the Origin header. That means no offline, no-account, no-approval path, which the working agreement forbids. Discord's own behaviour does restore voice settings when the controlling app disconnects. MicMix instead shows live Discord/FiveM running status and the exact manual menu path in the wizard, settings and footer. Revisit only if the user obtains Discord approval.
- Phase 4 verification passed: TypeScript/esbuild; npm run check:phase4 simulates a fresh install in an isolated userData profile (MICMIX_USERDATA), asserts config.json absent then wizard shown, walks the wizard (mic preselected to Yeti, test tone with master fader 0 kept master peak 0), assigns a pad and decodes it, rejects a bare-key hotkey, registers Ctrl+Shift+F9 globally, refuses pad playback OFF AIR, plays the pad LIVE (soundboard meter 0.50, master peak 0 while muted), tap-again and auto-end clear activePads, then a SECOND MicMix process on the same profile confirms no wizard, mic restored by label, music fader 0.42 restored, pad + hotkey restored and re-registered, queue restored, status OFF. artifacts/phase4-wizard.png and phase4-ui.png inspected.
- main.ts loads any scripts/smoke-<name>.cjs via --smoke-<name>; --smoke-phase4 spawns --smoke-phase4-restart on the same MICMIX_USERDATA profile. gpu_disk_cache errors during the restart run are the two processes sharing a GPU cache and are harmless.
- Phase 4 CHECKPOINT PASSED (2026-09-09): user confirmed the fresh-install setup wizard runs. Persistence/hotkey audible-in-Discord parts remain the user's to exercise but setup is confirmed.
- Phase 5 implemented (packaging + docs). STOP before the elevated installer run and reboot, which need the user. No installer has been built with the real driver and none run elevated by me.
- VB-CABLE redistribution VERIFIED against vb-audio.com/Services/licensing.htm: bundling the STANDARD VB-CABLE silently in a third-party installer is allowed as donationware provided the end user can identify it as a VB-Audio product AND can donate/pay. A+B and C+D may NOT be bundled. Silent switches: VBCABLE_Setup_x64.exe -i -h (install), -u -h (uninstall); Windows may still show a one-time driver-signing prompt and usually needs a reboot before all apps see the device.
- Phase 5 code: electron-builder NSIS config in package.json build{} (appId com.micmix.desktop, perMachine, oneClick false, extraResources build/vbcable -> resources/vbcable). build/installer.nsh runs VB-CABLE -i -h, net stop/start audiosrv, offers reboot; customUnInstall offers -u -h removal. build/LICENSE-notice.txt is the NSIS license page with VB-Audio donationware attribution. build/vbcable/README.txt tells the packager to drop the standard driver in (gitignored except README). Settings > About shows version + VB-Audio credit + Donate/vb-audio.com buttons (open:donate, open:vbcable via shell.openExternal). App version bumped to 1.0.0; react/react-dom moved to devDependencies (esbuild inlines them, so runtime node_modules is not needed).
- Phase 5 verification: TypeScript/esbuild pass. npm run check:phase5 confirms the About panel shows "About MicMix 1.0.0", VB-CABLE/VB-Audio credit, and a Donate button (artifacts/phase5-about.png). npx electron-builder --dir packaged MicMix.exe with app.asar containing all dist files and resources/vbcable created by extraResources; the packaged MicMix.exe boots to a titled window in <1s (7 processes). NOTE: launching MicMix.exe from this harness needs ELECTRON_RUN_AS_NODE cleared (the Claude Code env sets it, which otherwise makes the exe run as bare Node and exit 0 instantly); scripts/start.mjs already deletes it.
- The actual signed NSIS installer with the bundled driver, the elevated install, the audiosrv restart, and any reboot are the user's to run: download standard VB-CABLE from vb-audio.com/Cable into build/vbcable, then npm run dist:win, then run release/MicMix-Setup-1.0.0.exe as admin.
- README.md rewritten for the finished app: five-step quick start and troubleshooting for missing CABLE, silent Discord, robotic audio, double monitoring, and blocked embeds, plus a developer/build section.
- Post-1.0 tweak (user request, testing in FiveM): added monitorMusicVolume ("Music in my headphones", 0-1, default 1) so the operator can set music loudness in their OWN monitor independently of the outgoing mix. Implemented as a monitorMusic GainNode carrying music+pads into the monitor bus only; the outgoing (virtual) path is untouched. Verified offline: monitorMusicVolume 0.25 gives monitorMusicQuarterRatio 0.25 while the virtualOut music peak is unchanged (phase2-checks). Default duck depth softened -12 -> -8 dB so music stays more present under voice; the offline duck test now pins duckDb=-12 to test the math independent of the default. config.parseConfig now MERGES saved settings onto defaults, so a config saved before a new field loads and keeps the user's other tuning (verified: old config keeps music 0.42 and duckDb -12, fills monitorMusicVolume 1). NOTE: existing users keep their saved -12 dB duck; the softer default only affects fresh installs, so tell the user to lower "Music reduction" themselves if they want gentler ducking now. Source faders stay 0-100% (user chose not to add make-up gain above 100%).
- Phase 3 code: src/youtube-url.ts (URL parsing plus embed-error messages), src/youtube-view.ts (BrowserView lifecycle in main), src/youtube-preload.ts (IFrame API postMessage bridge, no filesystem or IPC exposed to the remote page), capture wiring in src/passthrough.ts, video panel and slot bounds in src/ui.tsx.
- Capture is verified, not assumed: scripts/capture-probe.cjs measured getDisplayMedia audio peak 0.49 from a BrowserView whose webContents was setAudioMuted(true), with enableLocalEcho false. No system-wide loopback is used; setDisplayMediaRequestHandler only ever grants the YouTube view's frame to the hidden worker frame.
- YouTube embeds run in their own session partition with permissions denied, downloads blocked, navigation blocked, window.open denied, audio muted. Player state, position, duration, title and error codes flow to the worker; the worker feeds the captured MediaStream into the existing music gain, so ducking, master, limiter, mono and monitor all apply. Error 101/150 shows "This video blocks embedding, try another".
- Video BrowserView bounds follow the UI slot via ResizeObserver, and detach when offscreen or smaller than 200x200.
- User report after Phase 3: YouTube audio in Discord mic test "plays and then stops" at low level. 45 s silent soak (npm run build && node scripts/start.mjs --smoke-youtube-soak, artifacts/youtube-soak.json) showed capture never dropped and the player never paused; music peak fell from ~0.6 to ~0.16 exactly when the Yeti was hot, i.e. auto-duck at the old -40 dBFS threshold. Fixes: defaults are now full level (mic/music/soundboard/master 1.0), duck threshold -30 dBFS, embed forced to unMute + setVolume 100 on ready. Discord Custom profile must have Noise Suppression None, Echo Cancellation off, AGC off, Input Sensitivity fully left or automatic; Krisp and the manual gate suppress music. Audible result still unconfirmed.
- Phase 3 CHECKPOINT PASSED on the core claim 2026-09-09: a friend in Discord heard the YouTube audio plus mic, loud. Remaining feedback: level "bounces" and an occasional brief pause.
- 60 s silent gap detector (node scripts/start.mjs --smoke-youtube-gaps, artifacts/youtube-gaps.json; mic 0 so no ducking) polled the embed's video.currentTime every 250 ms against the 20 Hz captured meter: 0 stalls, 0 jumps, >= 14.6 s buffered ahead, no exact-zero meter frames after playback started (quiet frames were content room tone ~7e-4). Capture path and YouTube buffering are ruled out; bouncing is the -12 dB duck plus limiter gain reduction when a hot Yeti and full-level music both hit the ceiling; residual pauses are attributed to Discord's network or the duck dip. Limiter release changed 50 ms -> 150 ms to reduce pumping. Buffering (player state 3) now sets AudioState.buffering and shows "Buffering..." beside the YouTube title.
- main.ts loads any scripts/smoke-<name>.cjs via --smoke-<name>.
- Phase 3 verification passed: TypeScript/esbuild; npm run check:phase3 (silent, master 0 and monitor off) loaded a real embed, captured audio peak 0.156, metadata title/duration 22:24, play, pause, seek to 10 s, auto-advance path, non-YouTube URL rejected, master peak 0 while muted, OFF AIR clean. artifacts/phase3-player.png shows the live embedded player. npm run check:capture and npm run check:youtube rerun the probes.
- Phase 2 graph is in src/mixer-graph.ts; lifecycle/local music in src/passthrough.ts. Independent source gains, duck gain, master, compressor, sample peak guard, optional mono, and per-source/master analysers. RMS and peak reports at 20 Hz; playback position at 4 Hz. Audio remains in hidden renderer.
- Compressor-only overload test FAILED above full scale. Added WaveShaperNode sample peak guard after compressor to cap at -1 dBFS, with an overload indicator. Normal samples below ceiling pass unchanged; overload should be corrected with faders. This is not an EQ feature.
- Monitor uses a second AudioContext with explicit non-cable sink, bridged from music plus optional mic via MediaStreamAudioDestinationNode. Default excludes mic. Master affects both paths; independent monitor volume/mute. OFF AIR releases mic, closes both contexts, pauses music and retains queue/position in memory. LIVE does not automatically restart music.
- Files are selected via dialog or dropped File objects through preload webUtils; main validates extensions/file existence and registers IDs. Enqueue IPC only accepts registered files. Worker owns each audio element and MediaElementAudioSourceNode; old sources/elements are disposed when changing tracks. Queue supports play/pause/seek/skip/select/remove/clear and auto-advance, capped at 500.
- Phase 2 verification passed: TypeScript/esbuild; real muted Electron playback of generated wav/mp3/flac/ogg; seek/pause/auto-advance/queue removals, corrupt file recovery, cancelled startup stays OFF AIR; meter reports and zero master signal during muted checks. Offline real Web Audio tests measured monitor-mic exclusion peak 0, duck ratio 0.2512 (-12 dB), release ratio 0.9513, identical mono channels, and overload output max 0.89125 (-1 dBFS). UI and settings screenshots inspected; visible UI and hidden audio worker assertions pass.
- Run npm run check:phase2 to reproduce. FFmpeg is used ONLY as a development fixture generator, already present on this machine; app runtime has no FFmpeg dependency and downloads nothing. Codec fixtures/reports/screenshots are ignored under artifacts/. Test selects this machine's Yeti and Realtek headset but mutes all physical outputs before creating live contexts. User must evaluate audible results.
- For Discord mic-test checkpoint, disable MicMix monitoring to prevent music being heard twice; then stop Discord mic test and test MicMix monitoring separately. Settings/queue persistence remains Phase 4.
- Phase 1 startup fix: user reported no visible window. Reproduced with ui.isVisible() false; changing launcher windowsHide from true to false made visibility assertion pass. Earlier capturePage screenshot did not prove visibility. Added visible/non-minimized UI and hidden-worker regression assertions, explicit show/focus after UI load and on second launch.
- Hidden audio worker owns exact-device mic capture with all three processing flags false, interactive AudioContext, explicit setSinkId to CABLE Input, master gain, and 440 Hz / 1.5-second / -20 dBFS peak test tone.
- OFF AIR cancels pending starts, releases tracks, and closes contexts. Device loss, context interruption, and sink changes stop output.
- Windows default recording device is now CABLE Output. The selector excludes cable and default aliases, so user must select their Yeti (or another physical mic). Explicit real default is auto-selected only when its group matches a non-cable device.
- Phase 1 validation: TypeScript/esbuild pass; silent Electron smoke checks pass (startup OFF AIR, stop IPC, reject tone while off, reject default mic and invalid commands, mic selector excludes cable, real silent setSinkId succeeds at 48000 Hz). Screenshot reviewed. No mic passthrough/tone audio was generated by these checks. Physical unplug and audible checks remain for user.
- Verified 2026-09-08: Node 22.23.2, npm 10.9.8, Git 2.40.1, Windows 11 Pro x64. Electron 44.3.0, React 19.2.8, TypeScript 5.9.3.
- TypeScript and esbuild pass. Real hidden Electron renderer enumerated 15 endpoint entries with no permission/enumeration error. Secure context and AudioContext.setSinkId supported. Default mic: Yeti Classic. Default playback: Realtek headset.
- Initial Phase 0 scan found cable missing; subsequent user log and Phase 1 smoke scan confirm both endpoints now present.
- Diagnostic report: ignored artifacts/devices.json. UI launched successfully, user visual confirmation pending. Run npm start to open it, npm run check:devices for console enumeration.
- Standard VB-CABLE now present for development. Bundled installer remains Phase 5.
- UI redesign (2026-09-09, user request for a freer, Apple-style look): frosted-glass dark theme in src/ui.css with system font stack, inline SVG icons (src/icons.tsx, no data: URIs because of the CSP), shared controls in src/controls.tsx (Level bar, iOS-style Switch, DeviceSelect). Horizontal faders with per-strip peak bars, duck switch, circular play button, YouTube/Local segmented source picker, previous-track button (select index-1), empty pads assign a clip on tap, status card with decorative master-driven wave plus mini meters and dB readout, Discord/FiveM/monitor chips. Settings is now a modal sheet; the YouTube BrowserView is hidden whenever the wizard OR settings sheet is open (both paint below a BrowserView otherwise). Window is frameless with Windows titleBarOverlay (hidden title bar, 40 px drag strip). All smoke selectors kept (.wizard, .wizard-nav, #microphone, #wizard-mic, .gear/[aria-label=Settings], .about h2/button, wizard button labels). node scripts/start.mjs --smoke-shot writes artifacts/shot-{wizard,main,settings}.png and reports scrollHeight vs innerHeight; the main screen fits 1400x920 without scrolling. Phase 2/3/4/5 checks all pass on the new UI.
- Marketing site (2026-09-09) lives in site/ as a separate Vite + React 19 + TypeScript + Tailwind v4 package (own package.json, not part of the Electron build). Single page: responsive animated header (active-link pill, glass on scroll, mobile drawer), hero with a CSS-drawn app mockup scaled to its container, integration logos (simple-icons; Teams hand-drawn), features, four-step guide, FAQ, download CTA, footer. Verified at 1280 and 390 px wide with no horizontal overflow. Restyled 2026-09-09 at the user's request to look less generated: flat navy background, one blue accent (#1478FF), 1 px borders, no gradient blobs/glows/glass/shimmer, native <details> FAQ, brand gradient kept only on the wordmark. Hero (2026-09-09, user: "use the actual product design") shows a REAL render of the app: scripts/site-shot.cjs loads dist/index.html in a hidden window with scripts/site-shot-preload.cjs, a fake window.micmix bridge that feeds a staged LIVE state (no devices, no audio), and writes a 2x PNG via the DevTools Page.captureScreenshot to site/public/screenshots/micmix-live.png. Regenerate after UI changes: npm run build && env -u ELECTRON_RUN_AS_NODE npx electron scripts/site-shot.cjs. The hero backdrop reuses the app's own three radial washes at low opacity, drifting slowly. Hero motion (user asked for animation 2026-09-09, Motion for React reinstalled): staggered text entrance, headline word cycling Discord/FiveM/Teams/any game, src/components/SignalFlow.tsx (SVG animateMotion dots from mic/music/pads through the MicMix mark to Discord/FiveM/any game, hidden below lg), and the product shot tilts flat on scroll via useScroll/useTransform. All of it respects prefers-reduced-motion. Header (user request 2026-09-09): fixed, transparent full-width at the top; once scrolled past 8 px (or with the mobile drawer open) it becomes a centred max-w-3xl frosted pill (bg-ink/70, backdrop-blur-xl, rounded-full on sm+, rounded-2xl on phones) via CSS transitions; the mobile drawer is a matching glass card under it. NOTE for smoke tests: hidden Electron windows never fire scroll events, so scroll-state checks need show:true. The user's email must NOT appear on the site (user instruction 2026-09-09); Support links to GitHub issues instead. Download link is a placeholder in site/src/site-config.ts until a release URL exists. Run: cd site && npm run dev; build output site/dist.
- Branding (2026-09-09, from the user's MicMix_Brand_Package): src/brand/ holds micmix-mark.svg (header + wizard mark), micmix-primary-1024.png (About artwork) and icon.png (BrowserWindow icon); scripts/build.mjs copies src/brand -> dist/brand so the renderer references ./brand/... (CSP forbids data: URIs). build/icon.ico is the installer/exe icon (package.json win.icon + nsis installerIcon/uninstallerIcon/installerHeaderIcon). Palette follows docs/BRAND_GUIDE.md: bg #071126, cyan #00E5FF, blue #1478FF, purple #A855FF, magenta #FF2BD6; the wordmark is white "Mic" + gradient "Mix". Verified: electron-builder --dir embeds the new icon in release/win-unpacked/MicMix.exe (artifacts/exe-icon.png) and dist/brand is inside app.asar. No tray exists in MicMix, so the package's tray-icon.png is intentionally unused. The untracked site/ folder was branded afterwards on the user's request: site/public/brand/ (favicon.ico, mark SVG, apple-touch-icon, PWA icons + site.webmanifest, OG image), index.html head links, LogoMark now an <img> of the mark, Tailwind palette in site/src/index.css moved to the brand colours, old site/public/favicon.svg removed. site/ remains untracked; committing it is the user's call.
- GitHub pipeline (2026-09-09, user request "make the github so users get updates and the site deploys"): repo userlaws/micmix. .github/workflows/ci.yml (typecheck+bundle app and site), site.yml (push to main touching site/ -> GitHub Pages; injects SITE_URL, VITE_BASE, VITE_DOWNLOAD_URL, VITE_RELEASES_URL, VITE_APP_VERSION), release.yml (tag v* -> windows-latest, downloads standard VB-CABLE pack 45 from download.vb-audio.com into build/vbcable, npm run dist:win (now --publish never, otherwise electron-builder tries to publish on its own when it sees a git tag and fails without GH_TOKEN) with CSC_IDENTITY_AUTO_DISCOVERY=false (unsigned), publishes release/MicMix-Setup.exe to a GitHub Release; refuses if tag != package.json version). artifactName is now the stable MicMix-Setup.exe so releases/latest/download/MicMix-Setup.exe always resolves. Release flow: npm version patch && git push --follow-tags.
- In-app UPDATER (deliberate deviation from the original "no auto-updater" line, at the user's explicit request 2026-09-09): src/updates.ts wraps electron-updater (runtime dependency, esbuild external, packed by electron-builder; package.json build.publish github/userlaws/micmix makes electron-builder write resources/app-update.yml and release/latest.yml; the workflow uploads latest.yml + blockmap; --publish never keeps electron-builder from uploading). main.ts checks 3 s after the UI loads and every 6 h when config.updateCheck (Settings > Diagnostics > "Update automatically", default on); download is automatic; install (quitAndInstall silent + relaunch) happens automatically only while OFF AIR and after setup, else the UI offers "Restart now"/"Later" and autoInstallOnAppQuit finishes it at quit. Manual "Check now" in Settings is internal (no browser). UI: UpdateCard toast (bottom centre; conic progress ring, shimmer bar, animated gradient border) plus an Updates row summary. MICMIX_UPDATE_SIMULATE=1 fakes the whole flow in dev (node scripts/start.mjs --smoke-shot writes shot-update-downloading/done.png). build/installer.nsh skips VB-CABLE when the VBAudioVACMME service key or C:Program FilesVBCABLE exists (verified on this PC) and shows no MessageBox when silent, so silent updates never re-run the driver or prompt for reboot. perMachine install means each update triggers one UAC prompt. v1.0.1 and earlier only show a link; the first self-update works from v1.0.2 onward.
- Site for GitHub Pages: vite.config.ts base comes from VITE_BASE, siteUrl from SITE_URL, version from VITE_APP_VERSION or ../package.json; LogoMark uses import.meta.env.BASE_URL; manifest icon paths are relative to the manifest. Hero mockup flicker fix: bars animate scaleY instead of height, blurred border underlay removed, scaled window promoted to its own layer (will-change/backface-visibility).
